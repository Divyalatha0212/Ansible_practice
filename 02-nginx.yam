- name: install and run nginx
  hosts: we
  become: yes # giving root access
  tasks:
  - name: installing nginx
    ansible.builtin.debug:
     name: nginx
     state: lates
  - name: start nginx
    ansible.builtin.debug:
     name: nginx
     state: started
     enable: yes   